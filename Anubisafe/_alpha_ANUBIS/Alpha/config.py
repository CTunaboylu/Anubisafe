# Requirements.txt


#Create a Virtual environment for Anubis:
#Make sure necessary packages exist
venv_inst_cmd = "python3 -m pip install virtualenv"
venv_setup_cmd = "python3 -m venv anubis"
init_env_cmd = "source anubis/bin/activate"

leaving_cmd = "deactivate"

# install all here 
mac_cmd = "brew install cavaliercoder/dmidecode/dmidecode"
debian_cmd = "sudo apt-get install dmidecode"
